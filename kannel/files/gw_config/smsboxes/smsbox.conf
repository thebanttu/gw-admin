{%- set gwname = salt['pillar.get']('kannel:data:gwname') -%}

include = "/etc/kannel/{{ gwname }}/smsboxes/core.conf"

group = smsbox
smsbox-id = main_box
bearerbox-host = localhost
bearerbox-port = 9500
sendsms-port = 8132
log-file = "/var/log/kannel/{{ gwname }}/smsbox.log"   
syslog-level = 1
mo-recode = yes

include = "/etc/kannel/{{ gwname }}/smsboxes/sms-service.conf"
