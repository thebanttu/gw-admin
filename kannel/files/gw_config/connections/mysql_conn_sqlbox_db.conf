{%- set host = salt['pillar.get']('kannel:data:mysql:host') -%}
{%- set user = salt['pillar.get']('kannel:data:mysql:username') -%}
{%- set pass = salt['pillar.get']('kannel:data:mysql:password') -%}

group = mysql-connection
id = sqlbox-db
host = {{ host }}
port = 3306
username = {{ user }}
password = {{ pass }}
database = gw
max-connections = 10
