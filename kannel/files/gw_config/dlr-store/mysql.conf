{%- set gwname = salt['pillar.get']('kannel:data:gwname') -%}

include = "/etc/kannel/{{ gwname }}/connections/mysql_conn_mydlr.conf"

group = dlr-db
id = mydlr
table = express_dlr_store
field-smsc = smsc
field-timestamp = ts
field-destination = destination
field-source = source
field-service = service
field-url = url
field-mask = mask
field-status = status
field-boxc-id = boxc
